@model IEnumerable<Bai2_LTWeb.Models.SACH>
@helper Truncate(string input, int length)
{
    if (input.Length < length)
    {
        @input
    }
    else
    {
        @input.Substring(0, length)<text>...</text>
    }
}
@{
    ViewBag.Title = "DanhMucSach";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>QUẢN LÝ THÔNG TIN SÁCH</h2>

<p>
    @Html.ActionLink("Thêm mới sách", "ThemMoiSach")
</p>
<table class="table">
    <tr>
        <th>
            Tên sách
        </th>
        <th>
            Giá bán
        </th>
        <th>
            Mô tả
        </th>
        <th>
            Ảnh bìa
        </th>
        <th>
            Ngày cập nhật
        </th>
        <th>
            Số lượng tồn
        </th>
        <th>
            Mã chủ đề
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TENSACH)
            </td>
            <td>
                @string.Format("{0:0,0}", item.GIABAN) VNĐ
            </td>
            <td>
                @Truncate(item.MOTA, 100)
            </td>
            <td>
                <img src="@Url.Content("~/Images/" + item.ANHBIA)" width="60" height="80" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NGAYCAPNHAT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SOLUONGTON)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CHUDE.TENCHUDE)
            </td>

            <td>
                @Html.ActionLink("Sửa", "SuaSach", new { id = item.MASACH }) |
                @Html.ActionLink("Chi tiết", "ChiTietSach", new { id = item.MASACH }) |
                @Html.ActionLink("Xóa", "XoaSach", new { id = item.MASACH })
            </td>
        </tr>
    }

</table>